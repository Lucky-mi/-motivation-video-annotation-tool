# 标注质量检查平台 V3 使用说明

## 🎯 主要功能

标注质量检查平台 V3 是一个专门用于审核和验证视频标注质量的工具，支持：

- ✅ **视频片段播放**：只播放标注的具体时间段（自动限制播放范围）
- ✅ **可视化时间轴**：直观显示所有标注片段的时间分布
- ✅ **详细标注信息**：完整展示所有标注内容和元数据
- ✅ **审核管理**：批准、需修改、删除等审核操作
- ✅ **JSON编辑**：直接在界面中编辑标注JSON数据
- ✅ **美观的UI**：现代化、响应式的用户界面

## 🚀 启动方法

### 方法1：使用启动脚本（推荐）

```bash
cd d:\Desire-VQA\video_anno
python run_reviewer.py
```

选择启动模式：
- **选项1 - 完整系统**：同时启动后端API和前端界面
- **选项2 - 仅前端**：只启动Streamlit前端（适合API已运行的情况）
- **选项3 - 仅后端API**：只启动FastAPI后端服务

### 方法2：直接启动前端

```bash
streamlit run frontend/annotation_reviewer_v3.py --server.port 8502
```

启动后访问：http://localhost:8502

## 📖 使用流程

### 第一步：选择标注文件

1. 在左侧边栏查看所有可用的标注文件
2. 文件名后显示审核进度，如 `[3/10]` 表示已批准3个，共10个片段
3. 使用搜索框快速查找特定文件
4. 点击文件名加载标注

### 第二步：查看视频片段

**核心功能：视频只播放标注的时间段**

- 视频播放器会自动：
  - ✅ 从片段开始时间开始播放
  - ✅ 到达结束时间自动暂停
  - ✅ 限制进度条在片段范围内
  - ✅ 播放结束后回到开始位置

- 显示信息：
  - 开始时间（绿色）
  - 结束时间（红色）
  - 片段总时长

### 第三步：审核标注内容

#### 片段类型

系统支持4种标注类型：

1. **🎯 动机分析 (desire_motivation)**
   - 强度（强/中/弱）
   - 马斯洛层级（生理/安全/归属/尊重/自我实现）
   - BDI组件（信念/欲望/意图）
   - 置信度（高/中/低）
   - 推理链条
   - 支撑证据
   - 替代解释

2. **🔄 欲望转变 (desire_transition)**
   - 转变类型
   - 转变前状态
   - 转变后状态
   - 触发事件
   - 行为标记
   - 心理解释

3. **📊 行为序列 (behavioral_sequence)**
   - 行为类别
   - 强度
   - 目标
   - 行为描述
   - 推断心理状态
   - 与欲望的关联

4. **❓ QA片段 (key_segment_qa)**
   - 难度等级
   - QA类型
   - 片段描述
   - 心理意义
   - 是否推荐用于QA

### 第四步：执行审核操作

#### 审核按钮

- **✅ 合理**：标注准确，批准通过
- **⚠️ 需修改**：标注有问题，需要修正
- **🗑️ 删除**：标注错误，建议删除
- **↩️ 重置**：清除审核状态

#### 添加备注

在审核备注框中输入：
- 具体问题描述
- 修改建议
- 其他说明

点击 **💾 保存备注** 保存。

### 第五步：编辑JSON（可选）

1. 勾选 **📝 JSON编辑** 复选框
2. 在右侧面板中编辑JSON数据
3. 点击 **💾 保存JSON** 保存修改
4. 系统会自动创建备份文件

## 🎨 界面功能详解

### 顶部工具栏

- **⬅️ 上一段** / **➡️ 下一段**：快速切换片段
- **📊 时间轴**：显示/隐藏时间轴可视化
- **📝 JSON编辑**：显示/隐藏JSON编辑器
- **片段选择下拉框**：快速跳转到指定片段

### 时间轴可视化

- 显示所有片段的时间分布
- 不同颜色代表不同类型：
  - 🟢 绿色：动机分析
  - 🔵 蓝色：欲望转变
  - 🟡 黄色：行为序列
  - 🟣 紫色：QA片段
- 当前片段有黑色边框高亮
- 鼠标悬停显示详细信息

### 统计面板

显示当前视频的审核统计：
- ✅ 已批准
- ⚠️ 需修改
- 🗑️ 待删除
- ⏳ 待审核

## 📂 文件结构

```
data/
├── annotations_test/        # 标注JSON文件
├── Youtube_videos/          # 视频文件
├── annotations_backup/      # 自动备份
└── review_status.json       # 审核状态
```

## ⚙️ 高级功能

### 批量操作

在统计面板点击 **批量操作**：
- **✅ 全部批准**：批准当前视频的所有片段
- **↩️ 全部重置**：重置所有审核状态
- **📥 导出审核结果**：导出JSON格式的审核报告

### 自动备份

每次保存JSON编辑时，系统会自动创建备份：
- 备份位置：`data/annotations_backup/`
- 命名格式：`{video_id}_{timestamp}.json`

### 审核状态持久化

所有审核状态保存在 `data/review_status.json`：
```json
{
  "video_id_segment_id": {
    "status": "approved",
    "note": "标注准确",
    "timestamp": "2025-12-12T10:30:00"
  }
}
```

## 🐛 常见问题

### Q1: 视频不能播放？

**原因**：视频文件路径不正确或文件不存在

**解决方案**：
1. 检查 `data/Youtube_videos/` 目录中是否有对应的视频文件
2. 确认JSON文件中的 `video_path` 字段是否正确
3. 支持的视频格式：`.mp4`, `.avi`, `.mov`, `.mkv`

### Q2: HTML代码直接显示在页面上？

**原因**：Streamlit版本或浏览器兼容性问题

**解决方案**：
1. 升级Streamlit：`pip install --upgrade streamlit`
2. 使用Chrome或Firefox浏览器
3. 清除浏览器缓存

### Q3: 视频播放范围不正确？

**原因**：JSON中的时间戳数据错误

**解决方案**：
1. 检查标注文件中的 `start_seconds` 和 `end_seconds`
2. 确保 `end_seconds > start_seconds`
3. 使用JSON编辑功能修正时间戳

### Q4: DOM错误 "removeChild"？

**原因**：Streamlit动态渲染导致的临时错误

**解决方案**：
- V3版本已修复此问题
- 如仍出现，刷新页面即可

## 📊 审核最佳实践

### 审核标准

1. **动机分析审核**
   - ✅ 推理链条逻辑清晰
   - ✅ 支撑证据充分
   - ✅ 马斯洛层级分类合理
   - ✅ 置信度评估准确

2. **欲望转变审核**
   - ✅ 触发事件明确
   - ✅ 转变前后状态对比清晰
   - ✅ 行为标记与转变相关

3. **行为序列审核**
   - ✅ 行为描述详细
   - ✅ 心理状态推断合理
   - ✅ 与欲望的关联说明充分

4. **QA片段审核**
   - ✅ 难度等级合理
   - ✅ 片段描述准确
   - ✅ 心理意义分析深入

### 审核流程建议

1. **第一遍快速浏览**：了解整体标注质量
2. **第二遍详细审核**：逐个片段仔细检查
3. **第三遍重点复核**：重点关注"需修改"的片段
4. **导出审核报告**：完成后导出审核结果

## 🔧 技术细节

### 视频片段播放实现

使用HTML5 `<video>` 标签 + JavaScript实现精确的片段控制：

```javascript
// 监听时间更新
video.addEventListener('timeupdate', function() {
    if (video.currentTime >= endTime) {
        video.pause();
        video.currentTime = startTime;
    }
});
```

### 系统架构

```
Frontend (Streamlit)
    ↓
Local File System
    ↓
JSON Annotations + Video Files
```

## 📝 更新日志

### V3 (2025-12-12)

- ✅ 修复HTML渲染问题
- ✅ 实现精确的视频片段播放
- ✅ 修复DOM错误
- ✅ 优化UI美观性
- ✅ 增强标注信息展示

### V2

- 时间轴可视化
- 批量操作
- JSON编辑

### V1

- 基础审核功能
- 标注查看

## 🙋 支持

如有问题或建议，请：
1. 查看本文档的常见问题部分
2. 检查 `data/annotations_backup/` 中的备份文件
3. 查看控制台错误信息

---

**祝使用愉快！** 🎉
