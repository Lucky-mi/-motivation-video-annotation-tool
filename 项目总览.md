# AI视频标注系统 - 项目总览

## 🎯 项目目标

构建一个专业的AI辅助视频标注系统，用于分析视频中人物的**动机(Motivation)**和**隐性渴望(Implicit Desire)**。

---

## 📁 项目结构

```
video_anno/
├── backend/                      # 后端代码
│   ├── ai_providers/            # AI Provider抽象层
│   │   ├── __init__.py
│   │   ├── base_provider.py    # 基类和工厂
│   │   ├── gemini_provider.py  # Gemini实现 ✅
│   │   ├── openai_provider.py  # OpenAI实现 ✅
│   │   └── prompt_templates.py # 专业Prompt模板 ✅
│   │
│   ├── api.py                   # 旧版API
│   ├── api_v2.py               # 新版完整API ✅
│   ├── models.py               # 数据模型 ✅
│   ├── batch_processor.py      # 批量处理 ✅
│   ├── user_manager.py         # 用户管理 ✅
│   └── ...
│
├── frontend/                    # 前端代码
│   ├── app.py                  # 旧版前端
│   ├── app_v2.py              # 优化版前端
│   └── app_complete.py        # 完整功能版 ✅
│
├── config/                      # 配置
│   └── config.py              # 配置管理
│
├── data/                        # 数据目录
│   ├── videos/                # 上传的视频
│   ├── keyframes/             # 提取的关键帧
│   ├── annotations/           # 标注JSON文件
│   └── users/                 # 用户数据
│
├── .env                        # 环境变量 (API Keys)
├── requirements.txt           # Python依赖
├── start_system.bat          # 一键启动脚本 ✅
├── 使用指南.md               # 详细使用文档 ✅
└── 项目总览.md              # 本文件 ✅
```

---

## 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                    用户界面 (Streamlit)                  │
│  - 视频上传  - 批量处理  - 检查标注  - 视频重命名      │
└─────────────────┬───────────────────────────────────────┘
                  │ HTTP请求
                  ↓
┌─────────────────────────────────────────────────────────┐
│              后端API服务器 (FastAPI)                     │
│  - RESTful API  - 文件管理  - 用户管理  - 批量队列     │
└─────────────────┬───────────────────────────────────────┘
                  │
        ┌─────────┴──────────┐
        ↓                    ↓
┌──────────────┐      ┌────────────────┐
│ AI Provider  │      │  数据存储      │
│  - Gemini    │      │  - Videos      │
│  - OpenAI    │      │  - Keyframes   │
│  - 可扩展    │      │  - Annotations │
└──────────────┘      └────────────────┘
```

---

## 🔑 核心功能模块

### 1. AI Provider抽象层

**位置**: `backend/ai_providers/`

**设计模式**: 工厂模式 + 策略模式

**支持的Provider**:
- ✅ Google Gemini (推荐)
- ✅ OpenAI GPT-4V
- 🔄 可扩展其他模型

**核心类**:
```python
# 基类
class BaseAIProvider(ABC):
    def analyze_video_comprehensive(...)  # 视频分析
    def analyze_single_frame(...)         # 单帧分析
    def test_connection(...)              # 连接测试

# 工厂
class AIProviderFactory:
    def create_provider(name, **kwargs)   # 创建实例
    def register_provider(name, class)    # 注册新Provider
```

**特点**:
- 统一接口，易于切换模型
- 自动解析JSON输出
- 错误处理和重试机制

---

### 2. Prompt工程

**位置**: `backend/ai_providers/prompt_templates.py`

**设计原则**:
1. **What vs Why 分离**: 客观描述 vs 主观推断
2. **显性 vs 隐性**: 表面动机 vs 深层渴望
3. **Evidence-based**: 每个推断都要有视觉证据
4. **结构化输出**: 严格的JSON格式

**Prompt类型**:
- `COMPREHENSIVE_VIDEO_PROMPT`: 完整视频分析
- `SINGLE_FRAME_PROMPT`: 单帧分析
- `THEME_EXTRACTION_PROMPT`: 主题提取

**示例Prompt片段**:
```
### 第二步: 深度动机分析 (Why It Happened)
1. **显性动机**: 人物表面上表现出的行为动机
   例如: "想要完成工作任务"

2. **隐性渴望**: 人物内心真正的深层需求
   例如: "渴望被认可,害怕失败带来的羞耻感"

3. **渴望分类** (基于马斯洛需求层次):
   - physiological: 生理需求
   - safety: 安全需求
   - belonging: 归属需求
   - esteem: 尊重需求
   - self_actualization: 自我实现
```

---

### 3. 批量处理系统

**位置**: `backend/batch_processor.py`

**功能**:
- 文件夹扫描
- 任务队列管理
- 异步并发处理
- 进度跟踪
- 失败重试

**任务状态**:
```python
class TaskStatus(Enum):
    PENDING = "pending"       # 等待中
    PROCESSING = "processing" # 处理中
    COMPLETED = "completed"   # 已完成
    FAILED = "failed"        # 失败
    CANCELLED = "cancelled"  # 已取消
```

**使用示例**:
```python
# 添加任务
task_id = batch_processor.add_task(video_path, provider="gemini")

# 添加文件夹
task_ids = batch_processor.add_folder(folder_path, provider="gemini")

# 启动处理
await batch_processor.process_queue(process_function)

# 查看状态
status = batch_processor.get_status_summary()
```

---

### 4. 数据模型

**位置**: `backend/models.py`

**使用**: Pydantic进行数据验证

**核心模型**:

```python
# What - 客观描述
class KeyframeAction(BaseModel):
    timestamp: float
    action_description: str
    visual_context: str
    objects: List[str]
    characters: List[str]
    scene: str
    confidence: float

# Why - 动机分析
class MotivationAnnotation(BaseModel):
    explicit_motivation: str      # 显性动机
    implicit_desire: str          # 隐性渴望
    desire_category: DesireCategory  # 类别
    motivation_type: MotivationType  # 类型
    implicit_level: int  # 1-5     # 隐性程度
    reasoning: str                 # 推理依据
    visual_cues: List[str]        # 视觉线索
    confidence: float

# 完整标注
class KeyframeAnnotation(BaseModel):
    frame_id: int
    frame_path: str
    action: KeyframeAction
    motivation: MotivationAnnotation

# 视频标注
class VideoAnnotation(BaseModel):
    video_id: str
    keyframes: List[KeyframeAnnotation]
    transitions: List[MotivationTransition]
    overall_trajectory: str
```

---

### 5. RESTful API

**位置**: `backend/api_v2.py`

**技术栈**: FastAPI + Uvicorn

**主要端点**:

| 端点 | 方法 | 功能 |
|------|------|------|
| `/health` | GET | 健康检查 |
| `/providers/list` | GET | 列出可用Provider |
| `/videos/upload` | POST | 上传单个视频 |
| `/videos/upload-folder` | POST | 批量上传 |
| `/analyze/video` | POST | AI分析视频 |
| `/batch/add-task` | POST | 添加批处理任务 |
| `/batch/start` | POST | 开始批处理 |
| `/batch/status` | GET | 获取批处理状态 |
| `/review/list` | GET | 获取标注列表 |
| `/annotations/{video_id}` | GET | 获取标注数据 |
| `/keyframes/{video_id}/{filename}` | GET | 获取关键帧图片 |
| `/videos/{video_id}/rename` | POST | AI生成重命名 |

**API文档**: http://localhost:8000/docs (Swagger UI)

---

### 6. 前端界面

**位置**: `frontend/app_complete.py`

**技术栈**: Streamlit

**页面结构**:

```
侧边栏
├── AI模型选择
├── 功能菜单
│   ├── 📤 上传视频
│   ├── 📁 批量处理
│   ├── 🔍 检查标注
│   └── ✏️ 重命名视频
└── 用户信息

主内容区 (动态切换)
├── 上传页面
│   ├── 文件选择器
│   ├── 分析选项
│   └── 结果显示
│
├── 批量页面
│   ├── 文件夹输入
│   ├── 队列管理
│   └── 进度跟踪
│
├── 检查页面
│   ├── 顶部导航 (上一个/下一个)
│   ├── 关键帧查看器
│   ├── 标注内容展示
│   └── 人工编辑
│
└── 重命名页面
    ├── 视频选择
    ├── AI生成主题
    └── 确认重命名
```

**交互特性**:
- Session State 管理状态
- 图片缓存优化性能
- 快捷键支持
- 实时进度更新

---

## 🎨 设计亮点

### 1. 分层架构

```
表现层 (Streamlit)
    ↓
业务层 (FastAPI)
    ↓
服务层 (AI Providers)
    ↓
数据层 (File System)
```

### 2. 可扩展性

**添加新的AI Provider只需3步**:
1. 创建新的Provider类 (继承BaseAIProvider)
2. 实现必要方法
3. 注册到工厂

```python
# 示例
class CustomProvider(BaseAIProvider):
    def analyze_video_comprehensive(self, ...):
        # 你的实现
        pass

AIProviderFactory.register_provider('custom', CustomProvider)
```

### 3. Prompt工程

**专业的心理学prompt**:
- 基于马斯洛需求层次理论
- 区分显性/隐性动机
- 要求提供视觉证据
- 结构化JSON输出

### 4. 用户体验优化

**前端优化**:
- 图片预加载和缓存
- 快速帧切换 (<0.1秒)
- 批量操作支持
- 连续检查模式

**后端优化**:
- 异步处理
- 并发控制
- 自动重试
- 错误恢复

---

## 📊 数据流

### 单视频分析流程

```
1. 用户上传视频
   ↓
2. 后端保存到 data/videos/
   ↓
3. 调用AI Provider分析
   ├─ Gemini: 直接分析视频文件
   └─ OpenAI: 先采样帧再分析
   ↓
4. AI返回结构化JSON
   ↓
5. 提取关键帧并保存到 data/keyframes/
   ↓
6. 保存标注到 data/annotations/
   ↓
7. 返回给前端显示
```

### 批量处理流程

```
1. 扫描文件夹
   ↓
2. 为每个视频创建Task
   ↓
3. 添加到队列
   ↓
4. 并发处理 (max_concurrent=2)
   ├─ Task 1: 处理中...
   ├─ Task 2: 处理中...
   └─ Task 3-N: 等待中...
   ↓
5. 逐个完成并保存
   ↓
6. 更新状态供前端查询
```

---

## 🔧 配置说明

### 环境变量 (.env)

```env
# AI Provider API Keys
GEMINI_API_KEY=your_key_here
OPENAI_API_KEY=your_key_here

# 系统配置
MAX_CONCURRENT_TASKS=2
DEFAULT_PROVIDER=gemini

# 路径配置
VIDEOS_DIR=data/videos
KEYFRAMES_DIR=data/keyframes
ANNOTATIONS_DIR=data/annotations
```

### 依赖包 (requirements.txt)

```
# Web框架
fastapi>=0.104.0
uvicorn>=0.24.0
streamlit>=1.28.0

# AI Provider
google-generativeai>=0.3.0
openai>=1.3.0

# 数据处理
pydantic>=2.4.0
python-multipart>=0.0.6
python-dotenv>=1.0.0

# 视频处理
opencv-python>=4.8.0
Pillow>=10.0.0

# 工具
requests>=2.31.0
```

---

## 🚀 快速开始

### 1. 安装依赖

```bash
conda create -n desire python=3.9
conda activate desire
pip install -r requirements.txt
```

### 2. 配置API Key

编辑 `.env`:
```env
GEMINI_API_KEY=你的API_Key
```

### 3. 启动系统

**方法一**: 双击 `start_system.bat`

**方法二**: 手动启动
```bash
# 后端
python -m uvicorn backend.api_v2:app --reload

# 前端
streamlit run frontend/app_complete.py
```

### 4. 访问

- 前端: http://localhost:8501
- 后端: http://localhost:8000
- API文档: http://localhost:8000/docs

---

## 📈 未来规划

### 短期 (1-2周)
- [ ] 完善错误处理
- [ ] 添加更多测试
- [ ] 性能优化
- [ ] 用户登录系统完善

### 中期 (1-2月)
- [ ] 支持更多AI模型 (Claude, LLaMA等)
- [ ] 标注协作功能
- [ ] 数据导出工具 (CSV, Excel)
- [ ] 统计分析仪表板

### 长期 (3-6月)
- [ ] 移动端支持
- [ ] 实时视频分析
- [ ] 多语言支持
- [ ] 云端部署方案

---

## 🐛 已知问题

1. **OpenCV在某些系统上可能有冲突**
   - 解决: 卸载其他opencv包，只保留opencv-python

2. **Gemini API在某些地区需要代理**
   - 解决: 配置系统代理或使用VPN

3. **大视频处理可能超时**
   - 解决: 预处理视频到合理大小 (<50MB)

---

## 📝 代码规范

### Python代码风格

- 遵循 PEP 8
- 使用类型提示 (Type Hints)
- 文档字符串使用Google Style

### 命名规范

- 类名: `PascalCase`
- 函数名: `snake_case`
- 常量: `UPPER_SNAKE_CASE`
- 私有成员: `_leading_underscore`

### 文件组织

```python
# 导入顺序
# 1. 标准库
import os
from pathlib import Path

# 2. 第三方库
import streamlit as st
from fastapi import FastAPI

# 3. 本地模块
from backend.models import VideoAnnotation
```

---

## 🤝 贡献指南

### 添加新功能

1. 在对应模块创建新文件/类
2. 更新 API 端点
3. 更新前端界面
4. 编写文档
5. 测试功能

### 报告Bug

提供以下信息:
- 操作步骤
- 错误信息
- 系统环境
- 日志输出

---

## 📚 相关资源

### 理论基础

- **马斯洛需求层次理论**: 动机分类依据
- **内在动机 vs 外在动机**: 动机类型区分
- **微表情识别**: 隐性线索检测

### 技术文档

- FastAPI: https://fastapi.tiangolo.com
- Streamlit: https://docs.streamlit.io
- Gemini API: https://ai.google.dev/docs
- OpenAI API: https://platform.openai.com/docs

---

## 📞 联系方式

项目维护者: [你的信息]

技术支持: [联系方式]

---

**项目版本**: v2.0.0
**最后更新**: 2024-11-18
**许可证**: MIT
