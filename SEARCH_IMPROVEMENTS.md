# 搜索功能改进说明

## 新功能：持续搜索直到找到足够的新视频

### 问题
之前的搜索逻辑：
```
搜索 10 个候选视频
  ↓
过滤（跳过已存在的）
  ↓
如果只剩 1 个新视频，就只返回 1 个 ❌
```

**结果**：即使设置 `limit=5`，如果大部分视频已存在，可能只返回 1-2 个新视频。

### 改进后
新的搜索逻辑：
```
搜索 limit * 3 个候选视频
  ↓
过滤（跳过已存在的）
  ↓
如果不够 limit 个新视频：
  ├─ 增加搜索范围（+ limit * 2）
  ├─ 继续搜索
  └─ 重复直到找到足够的视频
```

**结果**：确保返回 `limit` 个新视频（除非真的搜不到了）✅

## 改进细节

### 1. 动态搜索范围
```python
# 初始: limit * 3 个候选
fetch_count = limit * 3  # 如果 limit=5, 初始搜索15个

# 如果不够，增加范围
if len(links) < limit:
    fetch_count = fetch_count + limit * 2  # 增加10个
```

### 2. 最大搜索限制
```python
max_fetch = 200  # 最多搜索200个候选，避免无限搜索
```

### 3. 详细的跳过统计
```
📊 跳过: 已存在=6, 时长不符=3, 年龄限制=0, 不可用=1, 其他=0
```

新增 `时长不符` 统计，更清楚地了解过滤情况。

## 使用示例

### 场景1: 数据库已有很多视频
```python
# 配置
VIDEOS_PER_KEYWORD = 10

# 搜索 "people arguing"
# - 数据库已有 6 个相关视频
# - 旧逻辑：可能只返回 4 个新视频 ❌
# - 新逻辑：会继续搜索，返回 10 个新视频 ✅
```

### 场景2: 时长过滤严格
```python
# 配置
MIN_DURATION = 30
MAX_DURATION = 300  # 5分钟

# 搜索 "tv drama scene"
# - 很多电视剧片段超过5分钟
# - 旧逻辑：可能只返回 2-3 个符合时长的视频 ❌
# - 新逻辑：会继续搜索，返回足够数量的视频 ✅
```

## 配置建议

### 调整每个关键词的搜索数量
```python
# config/search_config.py

# 如果数据库已有很多视频，可以增加这个值
VIDEOS_PER_KEYWORD = 10  # 推荐 5-10
```

### 调整时长范围
```python
# 如果某些类型的视频普遍较长
MAX_DURATION = 600  # 增加到10分钟
```

## 性能影响

### 搜索时间
- **第一次运行**（数据库为空）：与之前相同
- **后续运行**（数据库有数据）：
  - 如果已存在视频少：几乎无影响
  - 如果已存在视频多：会增加搜索时间，但**确保找到足够的新视频**

### 示例对比
```
关键词: "people arguing"
目标: 5 个新视频
数据库已有: 4 个相关视频

旧逻辑:
  搜索 10 个候选
  跳过 4 个已存在
  返回 1-3 个新视频 ❌
  耗时: ~5秒

新逻辑:
  搜索 15 个候选（初始）
  跳过 4 个已存在
  不够5个，继续搜索25个候选
  返回 5 个新视频 ✅
  耗时: ~8秒（稍慢，但确保结果）
```

## 日志输出

### 之前
```
🔍 正在搜索: people arguing (Limit: 5, Duration: 30-300s)
  📊 跳过: 已存在=6, 年龄限制=0, 不可用=0, 其他=0
✅ 搜索完成，筛选出 1 个视频  ❌ 不够！
```

### 改进后
```
🔍 正在搜索: people arguing (目标: 5 个新视频, 时长: 30-300s)
  📊 跳过: 已存在=6, 时长不符=3, 年龄限制=0, 不可用=1, 其他=0
✅ 搜索完成，筛选出 5 个视频  ✅ 达到目标！
```

## 测试

运行测试脚本：
```bash
python test_search_continue.py
```

## 总结

✅ **优点**
- 确保找到足够数量的新视频
- 更详细的跳过统计（包括时长不符）
- 自动扩展搜索范围
- 避免无限搜索（最大200个候选）

⚠️ **注意**
- 如果数据库已有大量相关视频，搜索时间会稍长
- 如果真的搜不到足够的新视频，会警告并返回实际找到的数量
- 最大搜索数量限制为200个候选

🎯 **推荐设置**
```python
# config/search_config.py
VIDEOS_PER_KEYWORD = 10  # 每个关键词10个新视频
MIN_DURATION = 30
MAX_DURATION = 300  # 可根据需要调整
```
