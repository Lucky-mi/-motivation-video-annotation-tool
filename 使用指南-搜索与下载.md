# YouTube视频搜索与下载使用指南

## 📋 概述

本系统提供了灵活的YouTube视频搜索、下载和AI审核流程，支持两种工作模式：

1. **分离模式**（推荐）：先搜索保存结果，后下载审核
2. **完整模式**：一次完成搜索、下载、审核

## 🎯 推荐工作流程

### 方式1: 分离模式（推荐，避免速率限制丢失结果）

```bash
# 步骤1: 只搜索，保存结果
python scripts/run_search.py --search-only

# 步骤2: 从保存的结果下载和审核
python scripts/run_download_review.py
```

**优势：**
- ✅ 搜索遇到速率限制不影响已搜索的结果
- ✅ 可以多次运行下载，支持断点续传
- ✅ 可以先查看搜索结果，选择性下载

### 方式2: 完整模式（一气呵成）

```bash
# 搜索 + 下载 + 审核一次完成
python scripts/run_search.py
```

**适用场景：**
- 小批量视频（< 50个）
- 网络稳定
- 不太担心速率限制

## 📖 详细使用说明

### 1. 搜索脚本 (run_search.py)

#### 基本用法

```bash
# 查看帮助
python scripts/run_search.py --help

# 仅搜索模式
python scripts/run_search.py --search-only

# 完整流程模式
python scripts/run_search.py
```

#### 输出文件

搜索完成后会生成：
- `data/search_results.json` - 搜索结果（已按评分排序）
- 包含视频的URL、标题、时长、频道、评分等信息

#### 示例输出

```
================================================================================
🎬 YouTube视频搜索与审核系统
================================================================================

关键词集合: minimal (6个关键词)
每个关键词搜索: 10 个视频
预估总视频数: ~60 个

================================================================================
💡 运行模式: 仅搜索
================================================================================
  ✓ 搜索视频
  ✓ 智能评分预筛选
  ✓ 保存结果到 data/search_results.json
  ✗ 不下载视频
  ✗ 不进行AI审核

  💡 后续步骤:
     python scripts/run_download_review.py
================================================================================

⚠️  注意事项:
  • 搜索可能遇到YouTube速率限制（系统会自动处理）

确认继续? (y/n): y

📦 初始化组件...
  ✅ 评分系统已加载历史数据

================================================================================
📍 阶段 1/2: 批量搜索视频
================================================================================

[1/6] 搜索关键词: people arguing
  ✅ 找到 10 个视频

...

✅ 搜索完成: 总共 60 个，去重后 58 个

================================================================================
📍 阶段 2/2: 智能评分预筛选
================================================================================

⏭️ 跳过低分视频 (分数: 0.35): Video Title...
⭐ 保留 (分数: 0.82): High Quality Video...

📊 预筛选结果:
  - 保留: 45 个
  - 跳过: 13 个
  - 节省AI成本: 22.4%

💾 搜索结果已保存: data/search_results.json
```

### 2. 下载审核脚本 (run_download_review.py)

#### 基本用法

```bash
# 查看帮助
python scripts/run_download_review.py --help

# 处理所有搜索结果
python scripts/run_download_review.py

# 处理指定文件的搜索结果
python scripts/run_download_review.py --input my_results.json
```

#### 高级用法

**断点续传：**
```bash
# 从第30个视频开始处理
python scripts/run_download_review.py --start 30

# 只处理20个视频
python scripts/run_download_review.py --limit 20

# 从第30个开始处理20个
python scripts/run_download_review.py --start 30 --limit 20
```

**只下载不审核：**
```bash
python scripts/run_download_review.py --skip-review
```

#### 参数说明

| 参数 | 说明 | 示例 |
|------|------|------|
| `--input` | 搜索结果文件路径 | `--input data/my_search.json` |
| `--start` | 开始处理的索引（从0开始） | `--start 50` |
| `--limit` | 处理的视频数量 | `--limit 20` |
| `--skip-review` | 只下载不审核 | - |

## 🚨 遇到速率限制怎么办？

### 场景1: 搜索时遇到速率限制

如果使用了 `--search-only`，已搜索的结果已保存，不会丢失：

```bash
# 1. 已搜索的结果保存在 data/search_results.json
# 2. 等待速率限制解除（通常30分钟-1小时）
# 3. 继续下载
python scripts/run_download_review.py
```

### 场景2: 下载时遇到速率限制

系统会自动提示断点续传命令：

```
⚠️ 遇到速率限制

💡 提示: 已搜索的结果保存在 data/search_results.json
   可以等待后使用以下命令继续:
   python scripts/run_download_review.py --start 25
```

只需复制命令，等待后继续即可。

## 📊 智能评分系统

系统会自动从历史数据学习，对视频进行预评分：

### 评分维度

1. **频道历史表现**（权重40%）
   - 该频道以往视频的通过率
   - 样本数越多，置信度越高

2. **时长偏好**（权重20%）
   - 基于历史通过视频的时长分布
   - 接近理想时长的视频加分

3. **标题关键词**（权重20%）
   - 正面关键词：从通过视频学习
   - 负面关键词：从拒绝视频学习

4. **观看次数**（权重10%）
   - 高观看量视频加分
   - 低观看量视频减分

### 评分建议

- **score ≥ 0.7**: 优先下载（⭐标记）
- **0.4 ≤ score < 0.7**: 正常下载
- **score < 0.4**: 跳过（节省API成本）

## 📁 输出文件说明

### 1. search_results.json

搜索结果文件，包含：
```json
[
  {
    "url": "https://www.youtube.com/watch?v=...",
    "title": "Video Title",
    "duration": 120,
    "channel": "Channel Name",
    "view_count": 50000,
    "search_keyword": "people arguing",
    "pre_score": 0.75,
    "pre_recommendation": "priority",
    "score_reasons": ["频道历史通过率: 80%", "时长接近理想范围"]
  }
]
```

### 2. youtube_links.json

视频数据库，包含：
- 所有视频的URL和元数据
- 下载状态
- AI审核结果
- 频道信息

### 3. Youtube_videos/

下载的视频文件存储目录

## 💡 最佳实践

### 1. 大批量采集

```bash
# 步骤1: 先搜索所有关键词（不怕中断）
python scripts/run_search.py --search-only

# 步骤2: 分批下载（每次20个）
python scripts/run_download_review.py --limit 20
python scripts/run_download_review.py --start 20 --limit 20
python scripts/run_download_review.py --start 40 --limit 20
```

### 2. 测试新关键词

```bash
# 步骤1: 先搜索看看结果
python scripts/run_search.py --search-only

# 步骤2: 查看搜索结果
cat data/search_results.json | grep "title"

# 步骤3: 如果满意，下载前10个试试
python scripts/run_download_review.py --limit 10

# 步骤4: 满意后下载全部
python scripts/run_download_review.py
```

### 3. 只下载高分视频

```bash
# 搜索结果已按分数排序，直接限制数量即可
python scripts/run_download_review.py --limit 30
```

### 4. 网络不稳定环境

```bash
# 每次只处理5个，逐步推进
for i in 0 5 10 15 20 25 30 35 40 45; do
  python scripts/run_download_review.py --start $i --limit 5
  sleep 60  # 每批间隔1分钟
done
```

## 🔧 配置调整

编辑 `config/search_config.py`:

```python
# 关键词集合
KEYWORD_SET = "minimal"  # 或 "expanded"

# 每个关键词搜索的视频数
VIDEOS_PER_KEYWORD = 10

# 视频时长范围（秒）
MIN_DURATION = 30
MAX_DURATION = 300

# AI审核设置
ENABLE_AI_REVIEW = True
STRICT_MODE = True
AUTO_DELETE_REJECTED = True

# 并发审核数
AI_REVIEW_WORKERS = 5
```

## ❓ 常见问题

### Q1: 搜索结果保存在哪里？
A: `data/search_results.json`，已按评分排序，高分在前

### Q2: 如何跳过已下载的视频？
A: 系统会自动检测 `data/youtube_links.json` 中的记录并跳过

### Q3: 速率限制要等多久？
A: YouTube通常限制30分钟到1小时，系统会自动处理

### Q4: 可以修改评分阈值吗？
A: 可以，修改 `backend/video_scorer.py` 中的阈值

### Q5: 如何查看评分系统学到了什么？
A: 运行 `python backend/video_scorer.py --stats`

## 📞 支持

遇到问题请检查：
1. `log.txt` - 详细日志
2. `data/youtube_links.json` - 视频数据库
3. `data/search_results.json` - 搜索结果

系统会自动处理大部分错误，并给出提示。
